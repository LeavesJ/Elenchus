frame_code: embed_credentials_as_a_list
posture: founder_ceo
provenance:
  source_type: owned
  pointer: ADR-001; lessons R-149
screen:
  verdict: lift
  screen_action: surface
  mean_distinguishability: 2.33
  mean_preference: 1.0
  framed_preferred_count: 3
  data_ref: data/lift/screen_embed_credentials_as_a_list.json
gates:
  surface_independence: pass
  atomicity: pass
  orthogonality: pass
  falsifiable_application: pass
  trainable_cognition: pass
nearest_sibling: choose_the_failure_default_deliberately
separating_artifact: A recommendation to make a single-valued, un-updatable embedded
  anchor a collection on day one, justified by unrecoverable-once-shipped (not by
  anticipated multiplicity), while rejecting the heavyweight remote-update scheme
  — versus output that embeds the scalar and says "make it a list when a second value
  appears."
decision: admit_provisional
rationale: cleanest lift of the mine (lift, 3/3, mean_pref +1.0); two clean substance
  wins (emb_trust_root, emb_constant_vs_fetch) where the control over-built a remote-update
  channel or accepted a stranding scalar — exactly the immutability-after-ship gap.
  surface_independence adjudicated PASS by the founder (transfers to any lock-in-now
  decision).
admitted_as:
  experience_id: irreversible_anchor
  ledger_ref: veldra:embedded_anchor_lock_in
marginal_lift: pass
